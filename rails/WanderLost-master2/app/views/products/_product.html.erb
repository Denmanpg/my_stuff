<div class="col-md-3">
	<div class="thumbnail">
		<%= link_to product_path(product.id) do %>
			<%= image_tag product.image.url, size: 300 %>
		<% end %>
		<div class="caption">
			<h3><%= product.name %></h3>
			<p><%= product.description %></p>
		</div>
	</div>
</div>